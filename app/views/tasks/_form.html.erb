

<%= simple_form_for(@task) do |f| %>
  <%= f.input :title %>
  <%= f.input :details %>
  <%= f.check_box :completed if !@task.new_record? %>
  <%= f.label :Completed if !@task.new_record? %>
  <%= f.submit class: "btn btn-primary"%>
<% end %>


